# Provider-neutral AI boundary

The MVP keeps the AI integration behind a provider-neutral boundary instead of committing to a specific model or vendor. This preserves the ability to compare Indonesian-language quality, latency, cost, and usage limits before accepting provider lock-in.

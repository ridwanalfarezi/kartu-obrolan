# AI SDK through Vercel AI Gateway

The MVP implements its provider-neutral AI boundary with Vercel AI SDK and initially transports requests through Vercel AI Gateway. This gives one validated application interface, centralized usage visibility, and model switching without embedding provider-specific SDKs into the product; the default model will be selected separately after a live Indonesian-language comparison.

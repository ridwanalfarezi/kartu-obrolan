# React and Vite on Vercel

The MVP uses React with TypeScript and Vite for the mobile-first PWA, deployed with its AI server boundary on Vercel. This keeps the three-screen client experience small while Vercel Functions protect AI credentials and provide a direct path to production deployment; Cloudflare Workers remains a viable alternative if edge location or its pricing becomes a stronger requirement.
